@Observed
export class RecentVisitModel {
  id: string = ''              // 访问ID
  pageTitle: string = ''       // 页面标题
  pagePath: string = ''        // 页面路径
  visitTime: number = 0        // 访问时间戳
}
