@Observed
export class RecommendedHospitalModel {
  id: string = ''                  // 医院ID
  name: string = ''                // 医院名称
  distance: number = 0             // 距离（km）
  address: string = ''             // 医院地址
  level: string = ''               // 医院等级
}
