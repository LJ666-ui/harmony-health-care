第1周（Day1-Day7）：基础搭建与核心功能贯通

Day1 前端提示词（GlobalTheme适配+启动页重构）

提示词1（P0优先级页面GlobalTheme适配）：基于当前已完成的Appointments.ets、Medications.ets、AddAppointmentPage.ets页面，完成GlobalTheme标准化改造。要求：1. 顶部导入import { GlobalTheme } from '../global'; 2. 添加@StorageLink('isOldModeEnabled') isElderMode: boolean = false; 3. 在aboutToAppear()中初始化老年模式状态；4. 替换所有硬编码的fontSize、fontColor、backgroundColor、borderRadius、margin，严格遵循替换规则（.fontSize(24)→.fontSize(GlobalTheme.getFontSizeTitle(this.isElderMode))，.fontColor('#1677FF')→.fontColor(GlobalTheme.THEME_COLOR)等）；5. 适配老年模式切换，切换后字号、间距同步变化；6. 无编译错误，可直接在电脑模拟器运行。

对应功能：完成3个高频使用页面的样式统一，消除硬编码，实现老年模式同步适配，保证页面视觉风格统一。

提示词2（P1优先级页面GlobalTheme适配）：基于当前已完成的HospitalPage.ets、DepartmentPage.ets、RehabListPage.ets页面，完成GlobalTheme标准化改造，改造要求与提示词1完全一致，确保所有样式统一遵循GlobalTheme定义的颜色、字体、间距、圆角规范，适配老年模式，无硬编码残留，页面布局不偏移、不挤压。

对应功能：完成3个重要页面的样式统一，确保就医服务、康复训练模块页面风格一致，老年模式正常生效。

提示词3（P2优先级页面GlobalTheme适配）：基于当前已完成的DeviceDiscoveryPage.ets、RiskAssessmentPage.ets、ScienceDetailPage.ets、Rehab3DPage.ets页面，完成GlobalTheme标准化改造，改造要求与提示词1一致，重点修复Rehab3DPage.ets部分未适配的样式，确保所有页面切换老年模式后无错乱，样式统一。

对应功能：完成4个一般优先级页面的样式统一，覆盖设备、风险评估、科普、3D康复模块，实现全项目样式标准化第一步。

提示词4（启动页重构）：重构Index.ets页面，将其改造为星云医疗助手品牌启动页。要求：1. 页面居中展示应用Logo（文字Logo“星云医疗助手”即可，字体使用GlobalTheme标题字体）；2. 显示slogan“智慧医疗，守护健康”，字体适配老年模式；3. 实现2秒自动跳转逻辑，跳转至Login页面；4. 适配老年模式，字体大小跟随全局设置变化；5. 背景使用浅色系（GlobalTheme.BG_COLOR），视觉简洁专业；6. 无编译错误，可直接运行。

对应功能：替换原有启动页，打造专业品牌展示效果，实现自动跳转登录页，适配老年模式，提升用户体验。

Day2 前端提示词（Mock数据扩充+全局点击反馈）

提示词1（全页面Mock数据扩充）：基于当前所有前端页面，扩充Mock数据至真实化水平（纯前端本地模拟，不依赖后端）。具体要求：1. HealthRecords从4条扩充到12条（血压×2、血糖×2、心率×2、体温×2、体重×2、睡眠×2，含正常/异常值，每条添加真实感notes备注）；2. Appointments从2条扩充到6条（覆盖内科/外科/骨科/儿科/妇科/眼科，不同时间段，状态多样化）；3. Medications从2条扩充到8条（涵盖降压药/降糖药/维生素等，添加剂量、服用频率备注）；4. HospitalPage从6家扩充到10家（三甲4家+专科3家+社区3家，添加医院等级、地址）；5. ScienceListPage从8篇扩充到15篇（分心血管/骨科等分类，添加摘要）；6. PrivacyPage日志从5条扩充到10条（涵盖登录/查看/修改等操作，添加操作时间、设备信息）；7. DeviceDiscoveryPage从3台扩充到8台（涵盖手环/血压计等，添加设备型号、绑定状态）；8. RiskAssessmentPage从5项指标扩充到8项（新增BMI/骨密度等，添加参考范围）；9. RehabListPage从4门课程扩充到8门（涵盖颈椎/腰椎等，添加课程简介）；10. VoiceAssistantPage从5条指令扩充到10条（新增查药品/查医院等场景）；11. 所有数据时间戳符合逻辑，无未来日期，状态多样化；12. 不改变页面原有布局和样式，仅扩充本地Mock数据，可直接运行。

对应功能：丰富所有页面展示数据，提升演示真实性，避免演示时数据单薄，所有数据本地存储，不依赖后端。

提示词2（全局点击反馈动画）：为所有前端页面的可点击元素（按钮、卡片、列表项）添加统一的点击反馈动画。要求：1. 使用onTouch事件监听按下/抬起状态；2. 按下时触发缩放+透明度动画（缩放比例0.97，透明度0.9，动画时长150ms）；3. 抬起时恢复原状态，动画流畅无卡顿；4. 所有可交互元素（包括导航栏、按钮、卡片、列表项）均需添加，风格统一；5. 不影响原有功能和布局，适配老年模式，动画效果不夸张；6. 无编译错误，可直接运行。

对应功能：提升页面交互体验，让用户操作有明确视觉反馈，增强页面质感和易用性，纯前端实现，不依赖后端。

Day3 前端提示词（登录逻辑优化+首页数据渲染）

提示词1（登录逻辑优化）：修改Login.ets页面，优化前端本地登录逻辑（不依赖后端接口）。要求：1. 保留用户名、密码输入框，实现前端表单验证（用户名非空、密码长度≥6位）；2. 登录成功后，将模拟的用户信息（预设用户名、头像）存入AppStorage，用于后续页面展示；3. 添加登录状态判断，已登录状态下直接跳转至HomePage，未登录则停留在登录页；4. 添加Loading状态（点击登录后显示Loading组件，模拟加载效果）；5. 添加错误处理（账号密码为空、密码长度不足时显示提示）；6. 保持原有页面样式和老年模式适配，无编译错误，可直接运行。

对应功能：实现前端本地登录逻辑闭环，替代后端接口依赖，实现登录状态存储和判断，确保登录流程正常。

提示词2（首页数据渲染优化）：修改HomePage.ets页面，渲染本地预设的用户基本信息和首页统计数据。要求：1. 从AppStorage中获取预设的用户信息（姓名、头像、年龄等），渲染本地Mock的首页统计数据（今日步数/本周预约数/健康评分）；2. 添加Loading状态，数据加载中显示Loading组件，加载完成后渲染数据；3. 添加空状态处理，模拟数据加载失败时，显示EmptyState组件（提示“加载失败，请重试”）；4. 数据展示适配老年模式，字体、间距正常；5. 保持原有页面布局和样式，数据渲染正确，无编译错误，可直接运行。

对应功能：实现首页本地数据渲染，替代后端接口依赖，让首页展示完整的用户数据和统计信息，提升演示真实性。

Day4 前端提示词（健康记录+预约管理纯前端实现）

提示词1（健康记录页面纯前端实现）：修改HealthRecords.ets页面，基于本地Mock数据实现健康记录相关功能（不依赖后端）。要求：1. 渲染本地预设的健康数据列表（支持分页、按类型筛选，纯前端筛选逻辑）；2. 实现下拉刷新功能，集成PullRefresh组件，下拉时重新加载本地Mock数据，显示刷新动画；3. 实现新增健康记录功能，提交的表单数据存入本地AppStorage，与原有Mock数据合并；4. 添加空状态处理，无健康数据时显示EmptyState组件（提示“暂无健康记录，点击添加”）；5. 操作成功/失败时添加Toast提示（使用promptAction.showToast()）；6. 适配老年模式，下拉刷新动画、表单样式正常；7. 无编译错误，可直接运行，数据展示与操作正常。

对应功能：纯前端实现健康记录查看、新增功能，支持下拉刷新和空状态处理，数据存储在本地，不依赖后端，完善交互体验。

提示词2（预约管理页面纯前端实现）：修改Appointments.ets和AddAppointmentPage.ets页面，基于本地Mock数据实现预约相关功能（不依赖后端）。要求：1. Appointments.ets渲染本地预设的预约列表（支持按状态筛选，纯前端筛选逻辑），展示预约详情（医生、医院、时间、状态等）；2. AddAppointmentPage.ets实现表单提交功能，提交的预约数据存入本地AppStorage，与原有Mock数据合并，表单验证（必填项、日期合理性）；3. 实现取消预约功能，点击取消后弹出确认弹窗，取消后更新本地预约状态，操作成功显示Toast提示；4. 预约状态显示正确（待确认/已确认/已完成/已取消），适配老年模式；5. 添加Loading状态和Error处理，无预约数据时显示EmptyState组件；6. 无编译错误，可直接运行，预约创建、取消流程正常。

对应功能：纯前端实现预约管理功能，支持创建、查看、取消预约，操作流程闭环，数据存储在本地，不依赖后端。

Day5 前端提示词（健康数据可视化+语音助手增强）

提示词1（健康数据可视化图表）：在HealthRecords.ets页面顶部添加Canvas绘制的折线图，基于本地Mock数据实现健康数据趋势展示（纯前端实现）。要求：1. 绘制近7天血压/血糖趋势折线图，横轴为日期，纵轴为数值（数据取自本地Mock）；2. 支持点击数据点显示详细信息（数值、时间、备注）；3. 支持左右滑动切换时间范围（7天/30天/90天），切换时加载对应时间段的本地Mock数据；4. 图表样式统一医疗蓝主题，线条颜色为GlobalTheme.THEME_COLOR，数据点标注清晰；5. 适配老年模式，图表尺寸、文字大小同步放大，不模糊；6. 无卡顿、无渲染错误，可直接运行。

对应功能：纯前端实现健康数据可视化，让用户直观查看血压、血糖变化趋势，提升页面专业性和易用性，不依赖后端数据。

提示词2（语音助手页面增强）：修改VoiceAssistantPage.ets页面，纯前端实现语音助手交互，优化UI体验（不依赖后端接口）。要求：1. 预设常见指令的回复内容，用户输入（或点击指令）后，直接匹配本地预设回复，无需调用后端；2. 实现对话气泡UI，用户消息右对齐（背景色医疗蓝），AI回复左对齐（背景色浅灰）；3. 支持多轮对话历史记录显示，可滚动查看，历史记录存储在本地；4. 新增紧急求助特殊处理，点击“紧急求助”指令时，显示红色警告样式，添加“拨打120”按钮（仅模拟，无需真实拨号）；5. 保持原有语音悬浮球动画，状态文字（正在聆听.../识别中.../指令执行中）正常切换；6. 适配老年模式，对话文字、按钮尺寸放大；7. 无编译错误，可直接运行，对话流程正常。

对应功能：纯前端实现语音助手交互，优化对话UI，支持多轮对话和紧急求助场景，提升语音交互体验，不依赖后端。

Day6 前端提示词（下拉刷新+Toast消息提示）

提示词1（全局下拉刷新功能）：为8个列表页面集成PullRefresh组件，实现下拉刷新功能（纯前端，不依赖后端）。要求：1. 覆盖页面：HealthRecords、Appointments、Medications、HospitalPage、ScienceListPage、RehabListPage、DeviceDiscoveryPage、PrivacyPage；2. 下拉时显示统一的刷新动画（医疗蓝主题），1.5秒后重新加载本地Mock数据；3. 刷新成功后隐藏动画，显示“刷新成功”Toast提示；4. 适配老年模式，刷新动画尺寸、提示文字大小正常；5. 不影响页面原有布局和功能，无卡顿，可直接运行。

对应功能：实现全局列表页面下拉刷新，纯前端加载本地数据，让用户可模拟获取“最新数据”，提升交互体验和演示效果。

提示词2（全局Toast消息提示）：为所有操作型页面添加统一的Toast消息提示（纯前端实现）。要求：1. 覆盖所有操作场景：保存成功/保存失败、删除确认/删除失败、提交成功/提交失败、刷新成功/刷新失败等；2. 使用promptAction.showToast()方法，样式统一（背景色深色半透明，文字白色，字体适配老年模式）；3. 提示文字简洁明了（如“保存成功”“删除失败，请重试”），显示时长1.5秒；4. 所有操作按钮、表单提交、数据删除等场景均需添加，触发时机合理；5. 不影响页面操作，无遮挡关键元素，可直接运行。

对应功能：为用户操作提供明确反馈，提升交互体验，让用户清楚了解操作结果，纯前端实现，不依赖后端。

Day7 前端提示词（BUG修复+UI统一+跳转动画）

提示词1（UI BUG修复）：修复所有前端页面的UI BUG，确保页面展示正常。要求：1. 修复文字截断、布局偏移、按钮错位等问题；2. 确保所有页面老年模式切换后无错乱，字体、间距、按钮尺寸正常；3. 修复卡片、列表、表单等元素的样式不一致问题，严格遵循GlobalTheme规范；4. 补全缺省页和空状态页面的美化，添加统一的EmptyState插图和提示文字；5. 无布局挤压、无文字重叠，适配手机屏幕，可直接运行。

对应功能：解决所有UI层面的问题，确保页面视觉效果统一、美观，提升演示质感。

提示词2（页面跳转动画优化）：为所有页面路由跳转添加统一的淡入淡出动画（纯前端实现）。要求：1. 跳转时页面淡入（透明度从0→1），返回时页面淡出（透明度从1→0），动画时长300ms；2. 动画流畅无卡顿，不影响页面加载速度；3. 所有路由跳转（包括返回按钮、卡片点击、导航栏切换）均需添加，风格统一；4. 适配老年模式，动画效果不夸张，不影响视觉体验；5. 无编译错误，可直接运行。

对应功能：优化页面跳转体验，让页面切换更流畅、自然，提升应用整体质感。

第2周（Day8-Day14）：核心功能攻坚与创新亮点

Day8 前端提示词（转院申请页面开发）

提示词1（转院申请页面开发）：新建TransferApplyPage.ets页面，纯前端实现转院申请功能（不依赖后端）。要求：1. 页面布局：顶部Header（标题“转院申请”）、左上角返回按钮（可返回上一级，路由使用router.pushUrl规范）、表单区域、提交按钮；2. 表单字段：原医院（下拉选择，选项为本地预设的医院列表）、目标医院（下拉选择，选项为本地预设的医院列表）、转院原因（多行输入框）、上传附件（模拟，显示“点击上传病历”按钮，仅做UI展示，不真实上传）、提交按钮；3. 表单验证：原医院、目标医院、转院原因必填，日期合理性检查；4. 提交逻辑：点击提交后显示Toast提示，将表单数据存入本地AppStorage，跳转到转院申请列表页；5. 样式统一医疗蓝主题，适配老年模式，表单控件尺寸、字体放大；6. 底部Tab导航栏正常显示，不消失、不错位；7. 无编译错误，可直接运行。

对应功能：纯前端实现转院申请表单页面，支持用户提交转院申请，数据存储在本地，完成转院申请流程第一步，不依赖后端。

提示词2（转院申请列表页面开发）：新建TransferListPage.ets页面，纯前端展示转院申请列表（不依赖后端）。要求：1. 页面布局：顶部Header（标题“我的转院申请”）、左上角返回按钮、申请列表区域；2. 列表展示：渲染本地AppStorage中存储的转院申请数据，每条申请显示原医院、目标医院、申请时间、申请状态（待审核/已通过/已拒绝，状态为本地预设）；3. 支持点击列表项查看申请详情，支持按状态筛选（待审核/已通过/已拒绝，纯前端筛选）；4. 已通过的申请显示“数据同步中”/“同步完成”状态（纯前端模拟）；5. 加载本地存储的申请列表数据，添加Loading状态和空状态处理；6. 样式统一医疗蓝主题，适配老年模式，列表项点击有反馈动画；7. 底部Tab导航栏正常显示，无编译错误，可直接运行。

对应功能：纯前端实现转院申请列表展示，支持查看申请详情和状态筛选，数据存储在本地，让用户了解转院申请进度，不依赖后端。

Day9 前端提示词（病历查看页面+风险评估页面增强）

提示词1（病历查看页面开发）：新建MedicalRecordViewPage.ets页面，纯前端实现脱敏病历展示功能（不依赖后端）。要求：1. 页面布局：顶部Header（标题“病历详情”）、左上角返回按钮、病历信息展示区域；2. 展示内容：本地预设的患者基本信息（姓名脱敏，如“张*”）、诊断结论、治疗方案、检查报告；3. 敏感信息处理：姓名、电话、身份证号、地址等敏感信息显示“***已脱敏***”标记；4. 加载本地预设的病历数据，添加Loading状态和Error处理；5. 样式统一医疗蓝主题，适配老年模式，文字清晰、间距合理；6. 底部Tab导航栏正常显示，无编译错误，可直接运行。

对应功能：纯前端实现脱敏病历查看，保护用户隐私，同时让用户和医生可查看病历详情，适配跨院协同场景，不依赖后端。

提示词2（风险评估页面增强）：修改RiskAssessmentPage.ets页面，纯前端增强AI共病联合风险评估展示效果（不依赖后端）。要求：1. 加载本地预设的多维度风险数据（高血压+糖尿病+衰弱+肌少症+跌倒风险）；2. 使用Canvas绘制五边形雷达图，5个顶点分别代表5种风险，不同风险等级用不同颜色填充（低风险绿色、中风险黄色、高风险红色）；3. 雷达图下方显示综合风险评分（0-100分）、风险等级（低/中/高）和个性化干预建议文案（本地预设）；4. 适配老年模式，雷达图尺寸、文字大小放大，颜色对比度提升；5. 添加Loading状态，数据加载完成后渲染雷达图，无渲染错误；6. 无编译错误，可直接运行。

对应功能：纯前端实现多维度风险雷达图展示，让用户直观了解自身健康风险，提升AI评估功能的演示效果，不依赖后端。

Day10 前端提示词（停车场预约+反向寻车）

提示词1（停车场预约页面开发）：新建ParkingReservationPage.ets页面，纯前端实现停车场预约功能（不依赖后端）。要求：1. 页面布局：顶部Header（标题“停车场预约”）、左上角返回按钮、车位平面图区域、预约按钮；2. 车位平面图：展示楼层、区域、车位编号，可用车位绿色高亮，已占用车位灰色置灰，预约中车位黄色标记（状态为本地预设）；3. 加载本地预设的车位列表数据，实时更新车位状态（纯前端模拟）；4. 点击车位弹出预约确认弹窗（显示车位编号、楼层、预约时长），点击确认后更新本地车位状态，显示Toast提示；5. 样式统一医疗蓝主题，适配老年模式，车位标识清晰、按钮尺寸放大；6. 底部Tab导航栏正常显示，无编译错误，可直接运行。

对应功能：纯前端实现停车场车位预约功能，支持查看车位状态、预约车位，完成智慧停车场景演示，不依赖后端。

提示词2（反向寻车功能开发）：在ParkingReservationPage.ets页面添加反向寻车功能（纯前端模拟，不依赖后端）。要求：1. 页面添加“反向寻车”按钮，点击后显示从当前位置到预约车位的步行路线（本地预设静态路线图）；2. 展示路线指引（箭头、转向提示），纯前端绘制，不调用真实地图接口；3. 路线图适配老年模式，路线颜色清晰、文字放大；4. 支持缩放路线图，查看详细指引；5. 不影响原有车位预约功能，样式统一，无编译错误，可直接运行。

对应功能：纯前端实现反向寻车，帮助用户快速找到预约车位，完善智慧停车场景，提升用户体验，不依赖后端。

Day11 前端提示词（AR导航增强+3D康复页面增强）

提示词1（AR导航页面增强）：修改ARNavigationPage.ets页面，纯前端添加AR导航真实效果（纯UI模拟，不调用真实相机和后端接口）。要求：1. 背景使用深色半透明遮罩，模拟相机取景框效果；2. 在背景上叠加AR导航箭头、虚拟路标（如“门诊楼A座”），箭头跟随路线方向动态指向（纯前端动画）；3. 添加语音播报模拟（文字提示“前方左转进入门诊楼”“距离目标还有50米”，纯前端文字提示，不调用语音接口）；4. 顶部导航信息（目标科室、距离/时长）为本地预设，实时更新（纯前端模拟）；5. 底部路线指引文字同步更新，适配老年模式；6. 左上角返回按钮正常，底部Tab导航栏不消失；7. 无编译错误，可直接运行，导航效果流畅。

对应功能：增强AR导航视觉效果，模拟真实AR实景指引，提升3D空间化专项奖演示效果，纯前端实现，不依赖后端。

提示词2（3D康复页面增强）：修改Rehab3DPage.ets页面，加载本地3D康复动作模型，完善演示效果（纯前端，不依赖后端）。要求：1. 加载本地预设的glTF格式3D动作模型，无需调用后端接口；2. 实现动作播放控制（播放/暂停/上一动作/下一动作），进度条显示播放进度；3. 动作播放时，同步显示本地预设的动作要领文字提示（如“颈椎拉伸，缓慢转动头部”）；4. 支持模型旋转、缩放，查看动作细节；5. 样式统一医疗蓝主题，适配老年模式，控制按钮尺寸放大、文字清晰；6. 无模型加载卡顿，动作播放流畅，无编译错误，可直接运行。

对应功能：纯前端实现3D康复动作演示，加载本地3D模型，支持播放控制，完善3D空间化专项奖演示场景，不依赖后端。

Day12 前端提示词（科普文章增强+个性化推荐）

提示词1（科普文章页面增强）：修改ScienceDetailPage.ets页面，纯前端添加文章朗读功能（不依赖后端）。要求：1. 页面添加“朗读全文”按钮，点击后调用HarmonyOS TTS引擎，朗读文章全文（纯前端调用系统能力，不依赖后端）；2. 显示朗读进度条，支持暂停/继续/停止控制；3. 朗读时按钮状态切换（播放→暂停，暂停→继续）；4. 适配老年模式，朗读按钮尺寸放大，进度条清晰；5. 不影响文章阅读，朗读声音清晰，可调节语速（可选）；6. 无编译错误，可直接运行。

对应功能：纯前端实现科普文章语音朗读，方便老年用户阅读，提升应用易用性和人文关怀，不依赖后端。

提示词2（个性化推荐Tab开发）：在SciencePage.ets页面添加“为你推荐”Tab，纯前端实现个性化科普内容推荐（不依赖后端）。要求：1. 页面新增“为你推荐”Tab，与原有分类Tab并列；2. 基于本地预设的用户健康数据标签，匹配对应的推荐文章（纯前端筛选逻辑）；3. 文章列表展示推荐理由标签（如“因为您关注血压健康”，本地预设）；4. 列表项样式与原有科普列表统一，点击可进入文章详情页；5. 添加Loading状态和空状态处理，适配老年模式；6. 无编译错误，可直接运行，推荐数据展示正常。

对应功能：纯前端实现个性化科普推荐，模拟根据用户健康状况推送相关文章，提升AI智能化演示效果，不依赖后端。

Day13 前端提示词（古迹AI复原+实时监控面板）

提示词1（古迹AI复原页面开发）：新建AncientRestorationPage.ets页面，纯前端实现古迹AI复原演示功能（不依赖后端）。要求：1. 页面布局：顶部Header（标题“古迹AI复原”）、左上角返回按钮、图片上传区域、结果展示区域；2. 图片上传：支持相册选取或拍照（模拟，点击按钮弹出选择弹窗，仅做UI展示，不真实上传）；3. 上传后显示处理中动画（转圈+文字“AI正在复原...”，纯前端模拟处理过程），加载本地预设的复原对比图；4. 结果展示：原图与本地预设的复原图左右对比，标注“原图”“复原图”，支持缩放查看细节；5. 样式统一医疗蓝主题，适配老年模式，按钮、文字尺寸放大；6. 底部Tab导航栏正常显示，无编译错误，可直接运行。

对应功能：纯前端实现古迹AI复原演示，模拟图像增强和OCR识别效果，提升AI智能化专项奖演示亮点，不依赖后端。

提示词2（实时监控面板开发）：新建MonitoringDashboardPage.ets页面，纯前端实现患者实时监控功能（不依赖后端）。要求：1. 页面布局：顶部Header（标题“实时健康监控”）、左上角返回按钮、监控指标区域；2. 监控指标：实时心率曲线图（每5秒刷新一次本地预设数据点，纯前端模拟）、血氧饱和度仪表盘、今日步数环形进度条；3. 异常数据处理：心率/血氧超出本地预设正常范围时，对应指标红色闪烁警告，显示“异常预警”提示；4. 使用定时刷新模拟实时数据更新，不调用WebSocket接口；5. 样式统一医疗蓝主题，适配老年模式，图表、仪表盘清晰易懂；6. 底部Tab导航栏正常显示，无卡顿，可直接运行。

对应功能：纯前端实现患者实时健康监控，展示心率、血氧等指标，支持异常预警，完善全场景协同演示效果，不依赖后端。

Day14 前端提示词（UI统一+性能优化）

提示词1（新页面UI统一）：统一第2周新增所有页面（TransferApplyPage、TransferListPage、MedicalRecordViewPage等）的UI风格。要求：1. 严格遵循医疗蓝#1677FF主题，统一所有页面的圆角、间距、字体、按钮样式；2. 确保新增页面与原有页面视觉统一，无样式差异；3. 修复新增页面的布局偏移、文字截断问题；4. 适配老年模式，所有新增页面的字体、按钮尺寸同步放大；5. 无编译错误，可直接运行，视觉效果一致。

对应功能：确保全项目UI风格统一，提升应用整体质感，为大赛演示做好准备。

提示词2（AR/3D页面性能优化）：优化ARNavigationPage.ets和Rehab3DPage.ets页面的渲染性能（纯前端优化）。要求：1. 优化本地3D模型加载速度，添加模型预加载逻辑，目标加载时间<3秒；2. 优化AR导航箭头渲染，减少卡顿，确保动画流畅；3. 关闭不必要的渲染效果，降低内存占用；4. 适配不同手机屏幕，避免渲染错位；5. 无闪退、无卡顿，可长时间运行，可直接用于演示。

对应功能：提升AR和3D页面的运行稳定性和流畅度，避免演示时出现卡顿、闪退问题。

第3周（Day15-Day21）：专项冲刺 + 大赛提交准备

Day15 前端提示词（老年模式深度优化+多端自适应）

提示词1（老年模式深度优化）：对所有前端页面进行老年模式深度优化。要求：1. 所有页面支持超大字体模式，最大字体尺寸36px，跟随全局设置自动变化；2. 所有按钮高度增加至老年友好尺寸（最小60px高度），间距加大，避免误触；3. 新增高对比度配色方案，支持白底黑字/黄底黑字切换，在设置页面添加切换开关；4. 图标尺寸放大，文字加粗，提升可读性；5. 所有交互元素（按钮、卡片）的点击区域扩大，减少误触；6. 适配老年模式切换，所有页面无错乱，可直接运行。

对应功能：深度优化老年模式，提升老年用户使用体验，符合大赛人文关怀要求，增强应用竞争力。

提示词2（多端自适应布局优化）：优化所有页面的多端自适应布局（纯前端实现）。要求：1. 支持手机竖屏/横屏自适应，横屏时布局合理，无挤压、无错位；2. 支持平板适配，平板端显示2列/3列布局，充分利用屏幕空间；3. 支持折叠屏适配，展开/折叠状态切换时，页面布局自动调整，无错乱；4. 支持手表端简化界面，只保留核心功能（如健康数据查看、紧急求助）；5. 所有页面适配不同屏幕尺寸，无布局偏移，可直接运行。

对应功能：实现多端自适应，适配鸿蒙全场景设备，提升全场景协同专项奖演示效果。

Day16 前端提示词（UI细节打磨+无障碍功能增强）

提示词1（UI细节打磨）：对所有前端页面进行UI细节打磨，提升演示质感。要求：1. 统一所有页面的间距规范，严格遵循GlobalTheme定义的间距标准；2. 优化Loading动画效果，使用骨架屏替代传统菊花转，骨架屏样式统一医疗蓝主题；3. 优化空状态页面插图，绘制医疗风格SVG矢量插画，替代原有文字提示；4. 优化页面过渡动画，路由跳转使用自定义动画曲线，动画流畅自然；5. 统一所有按钮、卡片的圆角样式，避免样式混乱；6. 无视觉瑕疵，页面美观、专业，可直接用于大赛演示。

对应功能：打磨UI细节，提升应用视觉质感，给评委留下良好印象，增强大赛竞争力。

提示词2（无障碍功能增强）：增强所有页面的无障碍功能，适配特殊用户群体（纯前端实现）。要求：1. 支持屏幕阅读器（TalkBack），所有控件添加无障碍描述（如“提交按钮，点击可提交转院申请”）；2. 支持高对比度模式，切换后所有元素颜色对比度符合无障碍标准；3. 支持字体大小动态调整，添加滑块控制（11-36px），实时预览调整效果；4. 所有交互元素添加焦点提示，方便键盘操作；5. 适配无障碍操作逻辑，无操作障碍，可直接运行。

对应功能：增强无障碍功能，体现应用人文关怀，符合大赛社会责任要求，提升专项奖竞争力。

Day17 前端提示词（本地语音交互增强+多设备联动界面）

提示词1（本地语音交互增强）：修改VoiceAssistantPage.ets页面，纯前端优化语音交互效果（不依赖小艺SDK和后端）。要求：1. 实现语音波形动画，录音时显示声波跳动效果，模拟唤醒状态；2. 实时显示模拟的ASR中间识别结果，识别完成后匹配本地预设回复；3. 实现本地TTS语音播放（调用系统TTS引擎），与对话气泡同步显示；4. 支持核心医疗指令（挂号、查血压、查药品、紧急求助等），指令匹配准确；5. 适配老年模式，语音波形、文字提示清晰；6. 无编译错误，可在鸿蒙真机上测试运行。

对应功能：纯前端优化语音交互效果，支持语音控制核心功能，提升AI智能化专项奖演示效果，不依赖后端和外部SDK。

提示词2（多设备联动控制界面开发）：新建MultiDeviceControlPage.ets页面，纯前端实现多设备联动控制演示（不依赖后端和分布式接口）。要求：1. 页面布局：顶部Header（标题“多设备联动”）、左上角返回按钮、设备列表区域、流转控制区域；2. 设备列表：展示本地预设的周边鸿蒙设备（手机、平板、手表等），显示设备名称、设备类型；3. 流转功能：一键将当前内容（如康复课程、科普文章）推送到指定设备，显示流转状态（发送中/已接收/播放中，纯前端模拟）；4. 纯前端模拟设备发现和内容流转效果，不调用分布式软总线API；5. 样式统一医疗蓝主题，适配老年模式，按钮尺寸放大；6. 底部Tab导航栏正常显示，无编译错误，可直接运行。

对应功能：纯前端模拟多设备联动控制，展示鸿蒙超级终端接续能力，提升全场景协同专项奖演示效果，不依赖后端。

Day18 前端提示词（语音交互优化+全量UI回归）

提示词1（语音交互体验优化）：优化VoiceAssistantPage.ets页面的语音交互体验（纯前端）。要求：1. 添加语音引导提示语（如“请问有什么可以帮您？”），在无对话时显示；2. 添加常见问题快捷入口，点击气泡可直接发送常见指令（如“帮我挂心内科的号”）；3. 添加语音设置选项，支持调整音量、语速（纯前端控制）；4. 优化指令匹配逻辑，支持方言模糊识别（纯前端模拟）；5. 多轮对话上下文保持，可连续提问，无需重复唤醒；6. 适配老年模式，语音提示音量放大，文字清晰；7. 无编译错误，交互流畅，可直接用于演示。

对应功能：优化语音交互体验，提升语音功能的易用性，增强AI智能化专项奖演示效果，纯前端实现。

提示词2（全量UI回归测试与修复）：对所有30+前端页面进行全量UI回归测试，修复所有视觉问题。要求：1. 逐页检查所有页面的显示效果，确保样式统一、布局合理；2. 验证老年模式/普通模式切换流畅性，无错乱、无卡顿；3. 验证深色模式（如有）的显示效果，无颜色异常；4. 修复最后一批视觉问题（如文字错位、按钮颜色不一致、间距不合理等）；5. 确保所有页面底部Tab导航栏正常显示，无消失、无错位；6. 无编译错误，所有页面可正常运行，视觉效果完美。

对应功能：确保全项目UI无任何瑕疵，为大赛演示做好最终准备，提升演示效果。

Day19-Day21 前端提示词（最终优化+演示准备）

提示词1（最终性能优化）：对所有前端页面进行最终性能优化，确保演示稳定（纯前端优化）。要求：1. 检测并修复内存泄漏问题，确保应用长时间运行无卡顿、无闪退；2. 优化页面加载速度，首屏加载时间<2秒，子页面加载时间<1秒；3. 优化本地图片、3D模型加载，减少加载时间，添加加载失败降级方案；4. 优化动画效果，避免过度渲染，确保动画流畅；5. 压缩包体积，确保HAP包<50MB；6. 在鸿蒙真机和模拟器上测试，确保无任何性能问题，可稳定运行24小时以上。

对应功能：确保应用性能稳定、流畅，避免大赛演示时出现卡顿、闪退、加载缓慢等问题。

提示词2（演示场景优化）：优化大赛演示场景，确保演示流程流畅（纯前端）。要求：1. 预设本地演示数据，确保进入每个页面都有丰富的数据展示，无空状态；2. 优化演示流程，从登录→首页→核心功能→创新功能，流程顺畅，无多余操作；3. 为演示场景添加自动跳转逻辑（可选），方便快速演示；4. 确保所有创新功能（AR导航、3D康复、语音交互、多设备联动）可快速触发，演示效果清晰；5. 适配演示设备（真机），确保页面显示正常，无布局偏移；6. 无编译错误，可直接用于大赛现场演示。

对应功能：优化大赛演示体验，让演示流程顺畅、高效，突出核心创新点，提升评委评分。

提示词3（最终BUG清零）：对所有前端页面进行最终BUG排查，实现BUG清零。要求：1. 排查所有功能BUG（如按钮点击无响应、数据显示错误等）；2. 排查所有UI BUG（如布局偏移、文字截断、动画异常等）；3. 排查所有适配BUG（如老年模式错乱、多端适配异常等）；4. 确保所有页面无编译错误、无运行错误；5. 确保本地数据存储和读取正常，所有功能流程正常；6. 输出最终可运行的前端代码，可直接打包HAP用于大赛提交。

对应功能：确保应用无任何BUG，功能完整、稳定，达到大赛提交标准，为答辩做好准备。
